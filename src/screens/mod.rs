//! Screen implementations for Masteroids.
//!
//! Each screen implements the `Screen` trait and handles its own UI, updates, and
//! navigation commands (`ScreenCommand`).

pub mod start;
pub mod settings;
pub mod player;
pub mod host;
